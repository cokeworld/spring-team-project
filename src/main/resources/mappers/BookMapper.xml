<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.BookMapper">
	
	<insert id="addBook">
		INSERT INTO book (impUid, merchantUid, paidAmount, pgProvider, buyerName, paidAt) 
		VALUES (#{impUid}, #{merchantUid}, #{paidAmount}, #{pgProvider}, #{buyerName}, #{paidAt}) 
	</insert>
	
	<select id="getBookByNum" resultType="bookVo">
		SELECT * FROM book WHERE bookNum = #{num}
	</select>
	
</mapper>